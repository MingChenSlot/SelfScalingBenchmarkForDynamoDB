Provide dynamoDB access interfaces for our benchmark. 

Current available commands
    -i number_of_records        (insert specified number of records)
    -l                          (list all records)
    --count                     (return number of records in current table)
    --count-page                (the size of page is 1MB, return number of records in each page)
    -d                          (delete all records)
    -q --range_key FileName
                                (query specific record)
    -q -k key --range_key FileNameBeginWith
                                (query records with specific key and with file name started with provided string)
    -u --range_key FileName -s size_of_record
                                (update specific record with specified record size)
    --scan -s size_of_record
                                (scan and filter all records with specified size)

